# Maximum sum subarray problem
# Kadane's Algorithm O(n)

a = [-1, -2, 3, 4, -5, 6]
# sum of 8 (2, 5)

# max_sum = array of maximum sum subarray at each element (so far)
# max = what is the max sum subarray that we've seen so far


# def kadane(A):
#     max_so_far = 0
#     max_ending_here = 0
#
#     for i in range(1, len(a))
